namespace Sylves
{
    /// <summary>
    /// Represents a particular corner of a generic cell.
    /// The enum is empty - to work with corners, you need to either:
    /// * Use the methods on <see cref="ICellType"/>.
    /// * Cast to the enum specific to a given cell type, e.g. <see cref="CellCorner"/>.
    /// </summary>
    public enum CellCorner
    {

    }
}
